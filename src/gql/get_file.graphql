query GetFile($fileId: Int!) {
  getFile(fileId: $fileId) {
    id
    path
    fileName
    fileType
    codec
    bitrate
    samplerate
    channels
    duration
    fingerprint
    trackId
    createdAt
    updatedAt
    stage
  }
}
