mutation UpdateFile($fileId: Int!, $input: FileInput!) {
  updateFile(fileId: $fileId, data: $input) {
    id
    path
    fileName
    fileType
    codec
    bitrate
    samplerate
    channels
    duration
    fingerprint
    trackId
    createdAt
    updatedAt
    stage
  }
}
