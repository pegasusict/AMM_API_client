mutation UpdateUser($userId: Int!, $input: UserInput!) {
  updateUser(userId: $userId, input: $input) {
    id
    username
    email
    first_name
    middle_name
    last_name
    role
    created_at
    updated_at
    is_active
  }
}
