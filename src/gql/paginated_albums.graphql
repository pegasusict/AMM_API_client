query PaginatedAlbums($limit: Int!, $offset: Int!) {
  albums(limit: $limit, offset: $offset) {
    items {
      id
      title
      releasedate
      genres
      label
      track_count
    }
    total
  }
}
