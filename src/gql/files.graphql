query Files($limit: Int!, $offset: Int!) {
  files(limit: $limit, offset: $offset) {
    id
    path
    fileName
    fileType
    codec
    bitrate
    samplerate
    channels
    duration
    fingerprint
    trackId
    createdAt
    updatedAt
    stage
  }
}
